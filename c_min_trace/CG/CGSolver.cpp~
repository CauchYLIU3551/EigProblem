#include "CGSolver.h"

CGSolver::CGSolver():
  is_initialized(false),
  toler(1.e-12)
{};

CGSolver::CGSolver(const Matrix& A, const Matrix& M, double tol){};

void CGSolver::reinit(const Matrix& A, const Matrix& M)
{
  is_initialized=true;
};

CGSolver::~CGSolver()
{
  clear();
}

void CGSolver::clear()
{
  
}
